<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingVertical="12dp">

    <!-- Phase 31w：主標 Android permission 常數名（去 android.permission. 前綴），
         monospace 等寬 + selectable 方便長按複製 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/text_permission_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="monospace"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textStyle="bold"
            android:textIsSelectable="true"
            tools:text="BIND_NOTIFICATION_LISTENER_SERVICE" />

        <TextView
            android:id="@+id/text_permission_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textAppearance="?attr/textAppearanceLabelMedium"
            tools:text="已授權" />

    </LinearLayout>

    <!-- Phase 31ad：對齊 Detail 頁 pattern — info icon 用 drawableEndCompat 內嵌
         在 desc TextView 右側，自動跟文字行高匹配；整個 TextView 可點觸發詳情 dialog。
         省獨立 ImageButton 的大 touch target / padding 空間。 -->
    <TextView
        android:id="@+id/text_permission_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:gravity="center_vertical"
        android:drawablePadding="4dp"
        app:drawableEndCompat="@drawable/ic_info_outline"
        android:background="?attr/selectableItemBackground"
        android:textAppearance="?attr/textAppearanceBodySmall"
        android:textColor="?android:attr/textColorSecondary"
        tools:text="核心通知監聽功能" />

</LinearLayout>
